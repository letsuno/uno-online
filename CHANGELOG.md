# Changelog

本文件记录项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)。

## [0.15.0-beta.2] - 2026-08-19

这是 0.15 系列的第三个测试版，修复作弊终止提示提前消失的问题，并把 Redis 运行时结构切换改为由代码自动管理。

### 优化

- 作弊终止纳入统一的房间成员生命周期：服务端先提交房间解散，再向客户端发送明确的终止原因，客户端清理房间状态时不会连带关闭提示。
- 移除部署环境中的 `RUNTIME_SCHEMA_VERSION`。服务端以代码内的运行时代际为唯一事实来源；代际变化时会在启动阶段自动且原子地清理 UNO Redis 运行时状态，匹配时继续保留房间与对局。
- Redis key 使用固定的 `uno:runtime:` namespace，仅隔离本应用的运行时数据，不再维护旧 schema namespace 或兼容分支。

### 修复

- 修复从管理后台标记作弊、解散作弊房间等场景中，作弊提示未等玩家确认便被房间清理流程自动关闭的问题。
- 修复房间删除尚未成功时就可能发送终止提示，以及玩家过早点击继续造成客户端状态与服务端提交顺序竞争的问题。

### 部署注意

- Socket 协议版本从 1 提升到 2，旧页面会被要求刷新，不提供旧事件或旧 ACK 的降级兼容。
- 首次部署本版本时，服务端会自动清空现有 UNO Redis 房间、座位和对局状态并写入运行时代际；SQLite 中的用户、API Key 和 Passkey 数据不受影响。
- 本测试版不会更新 Docker `latest`；生产测试通道使用 `beta` 或精确的 `v0.15.0-beta.2` 镜像。

## [0.15.0-beta.1] - 2026-08-18

这是 0.15 系列的第二个测试版，重点完善管理后台、发布通道切换与 Beta 到正式版的更新日志流程。

### 新增

- 重构管理后台，新增服务器运行信息、账号凭据统计、房间座位与规则、对局阶段、AI 引擎能力和加载状态。
- 用户管理新增在线状态、连接数量、当前房间、最近登录时间和 API 密钥最近使用时间；SQLite 自动迁移并保留用户数据。
- Compose 镜像支持 `latest`、`beta` 和精确版本切换，Komodo 可按当前通道自动检查更新并一键切换测试版或正式版。
- 新增正式版差异检查命令，Beta 转正式版时自动汇总上一个正式 Tag 到当前分支的提交、差异统计和文件清单。

### 优化

- 管理后台统一中文并改为紧凑、信息优先的传统管理界面，删除宣传文案、装饰卡片和不必要的自定义控件。
- 自动发版支持从默认分支恢复已有 Tag；Beta 制品统一更新 Docker 与 npm 的 `beta` 通道。
- 正式版发布校验会阻止前端保留同版本 Beta 日志，确保 Beta 日志只在测试期间展示。

### 部署注意

- 本版本不修改 Redis 运行时结构和 Socket 协议，可从 `v0.15.0-beta.0` 兼容更新并保留活跃房间与现有 `JWT_SECRET`。
- 用户表会新增可空的 `last_login_at` 字段，服务启动时自动迁移，不清空或重建 SQLite 数据。
- 本测试版不会更新 Docker `latest`；测试环境使用 `beta` 或精确的 `v0.15.0-beta.1` 镜像。

## [0.15.0-beta.0] - 2026-08-18

这是 0.15 系列的首个公开测试版，集中验证社区 AI 插件、房间成员生命周期重构、服务重启恢复和自动发版链路。测试版不建议直接替换仍有活跃对局的生产环境。

### 新增

- 新增可扩展的社区 AI Provider：插件可以声明所需场内信息，结合 TypeScript 逻辑与 ONNX 推理选择合法操作；管理后台支持加载、启停和管理 Provider。
- 房间内添加机器人时可选择已启用的社区 AI 插件，并展示对应名称、头像和难度信息。
- Redis 运行时状态改用带 schema 版本的 namespace，服务端补齐优雅停机、快照刷盘和启动恢复；同 schema 的后续兼容更新可在重启后恢复活跃房间。
- 新增 PR/main CI 与 SemVer Tag 自动发版，预发布会生成版本化 Docker 镜像、npm MCP 包和 GitHub prerelease，但不会更新 Docker `latest`。

### 优化

- 重构房间成员、座位、观战、离房和重连生命周期，统一并发锁、提交顺序、名册对账与离线清理，减少幽灵成员和状态分叉。
- 简化客户端与 MCP 的 ACK、断线恢复、主动离开、暂停/观战队列、登录态和语音重连路径；协议不匹配时明确要求刷新，不保留旧前端降级分支。
- 为机器人和托管操作新增纯规则引擎合法动作枚举与有界循环保护，避免复杂村规组合下重复执行或无限循环。

### 修复

- 修复 10 人满员房间中房主以观战身份进入结算阶段后无法移除成员、移交房主或继续下一轮的死锁。
- 修复牌堆耗尽、手牌上限、累计惩罚和末牌限制组合可能导致回合卡死的问题，并修正 +4 复仇、叠加、反弹与免质疑组合的惩罚结算。
- 修复并发抢座、成员离房、房主迁移、观战队列和服务重启过程中可能出现的幽灵座位、残留在线状态与重复驱动。

### 部署注意

- 从 `v0.14.0` 或更早版本首次升级到本测试版属于运行时结构破坏性升级：旧服务仍运行时先排空活跃房间，停止旧服务后再清理旧运行时数据并部署；本版本不会读取或迁移旧的无 namespace Redis key。
- 本测试版不会更新 Docker `latest`；测试时请显式使用 `v0.15.0-beta.0` 镜像和 `@uno-online/mcp@0.15.0-beta.0`。

## [0.14.0] - 2026-07-27

本版本为全生命周期审计成果:对连接 / 房间 / 对局 / 观战链路做了三轮对抗性审计(8 维度扫描 → 逐条核实 → 修复复核),确认并修复 43 项缺陷,覆盖幽灵玩家、断线状态异常、并发竞争与重启恢复等所有已知问题。

### 新增

- 断线恢复能力:重连次数耗尽后,对局遮罩与等待室均提供「重新连接」按钮;网络恢复(`online` 事件)自动重连并回到对局——此前断网约 40 秒以上会永久卡死在遮罩后。
- 等待室新增连接状态横幅,断线不再静默失联。
- 多标签页会话接管:新标签页登录会接管会话,被接管的标签页安静退场——不再出现两个标签页互踢死循环,也不再误清对方标签页的登录凭证。
- 服务端新增 46 条 WebSocket 生命周期回归测试(内存 socket.io 双件驱动真实 handler 栈)与实机浏览器 e2e 脚本(`packages/e2e/lifecycle-verify.mjs`)。

### 优化

- 全员掉线宽限:对局中所有人同时断网(如同一 WiFi 闪断)现享有完整 5 分钟重连宽限——此前首个掉线者的 30 秒定时器就会把房间连对局一起解散;有队友仍在断线宽限期时,主动离开也不再立即解散对局。
- 服务器重启恢复:玩家重连即恢复回合驱动、下一轮投票、blitz 总时限与房主自动移交;启动时对账所有座位/观战者的在线标志——重启不再造成对局冻结、投票死锁或幽灵在线玩家。
- 对局中按浏览器返回键回到房间页会直接送回对局,不再被服务端当作"重连"(误关托管、撤销下一轮投票)。
- REST 与 socket 的登录态统一以本页内存为准:另一标签页换账号后,本页不再出现"牌桌是 A、资料是 B"的身份分裂。
- 下一轮自动投票语义收紧:只代表真正无法点击的人(bot / 离线 / 已淘汰)投票,在线开托管的玩家仍需自己确认下一轮。
- 游戏对局快照的保存时长与房间寿命对齐(1 小时 → 2 小时),重启后晚归的玩家不再遇到"房间还在、对局已丢"的状态。

### 修复

- 幽灵玩家 / 幽灵座位:
  - 抢座失败(与他人或"添加人机"并发)后观战者身份丢失,从所有人的列表中消失且再也无法入座。
  - 断线清理定时器被无关重连误取消,留下永久幽灵座位;被踢的离线成员重连后被大厅自动拽回原房间。
  - 服务器重启后快照里的 `connected:true` 幽灵在线玩家永不掉线、永不自动投票,导致下一轮投票永久卡死(二人局连踢人解锁都不可能)。
  - 房主离开时若其余成员都在断线宽限期,房主权悬空指向已移出的用户,房间从此无法开始/踢人/解散。
- 对局卡死:
  - bot 抢出(jump-in)打出最后一张牌获胜时不广播终局事件,房间软锁在无结算面板的回合结束状态。
  - 托管代打获胜、回合超时获胜等驱动路径同样漏发终局事件的问题。
  - 服务器重启后快照恰好轮到离线玩家时整局永久冻结(无回合计时器、无人可自救);仅观战者归队时棋盘无驱动。
  - 残留的 bot 思考定时器在真人接手回合后触发,强制替未超时、未托管的真人出牌。
  - blitz 模式:首轮结束后总时限静默失效;时限到点恰逢结算面板时被绕过;重启后时限丢失;blitz 终局叠加房主离线时记分板永久锁死。
- 并发竞争:
  - 「开始游戏」双击可各自通过校验并发两副牌(全桌手牌闪变、定时器双挂)。
  - 「开始下一轮」在全员淘汰/全 bot 局中双击导致跳轮、双重发牌。
  - 排队观战者转正与"添加人机"抢同一空位时异常中断开局流程,对局卡在回合结束且投票状态被清空。
  - 房间解散与仍在执行的定时器回调交错,把已解散对局的状态重新写回存储、重建残缺的房间键并永久堆积(新增 tombstone 拒写 + 空闲清扫兜底回收 + 损坏房间键防御性解析)。
  - 双驱动(托管轮询与回合计时器)并发时终局事件被重复广播、刚投的票被清掉。
- 跨房间 / 跨账号泄漏:
  - 聊天记录与对局日志泄漏进下一个房间的聊天面板。
  - 登出或凭证过期不清对局状态:换账号后打开对局页会看到上一账号的完整手牌并永久冻结。
  - 断线期间错过"房间解散"/"返回房间"广播的玩家,重连后停留在已不存在的对局画面。
  - 过期标签页重连失败时误清另一标签页正在使用的共享登录凭证。
- 其他:观战者队列在成员掉线/被踢后残留;`seat:take` 换座冷却与投票状态在房码复用时泄漏;等待阶段与对局阶段的观战者列表语义错位。

## [0.13.1] - 2026-07-26

### 优化

- 人机策略增强：
  - normal / hard 人机学会自查 +4 合法性：手握当前颜色时基本不再打出可被必胜质疑的 +4（此前 hard 这类出牌约占其 +4 的六成）；novice / easy 保留该失误作为拟人化。
  - hard 残局求解改为在全部胜线起手牌中择优，不再因手牌顺序放弃必胜线（如先出 +2 必胜时不再去赌数字牌）。
  - 同数字连打按颜色排序：零散色先甩、主色收尾定场上颜色。
  - +4 质疑判定改为直接定位出牌者，修复同牌抢出 +4 场景下可能认错对象的隐患。
  - 万能牌选色平局时随机（不再固定偏红）；多个人机同时可抢出时随机化竞速优先级（不再固定前排座位优先）。

### 修复

- 淘汰制：已淘汰玩家的座位在后续轮次仍会获得回合并摸牌行动的问题（回合轮转、禁止/+2/+4 目标、每轮起始座位现在全部只落在存活玩家上）。
- 淘汰制连锁问题：0 牌轮转不再把手牌传进已淘汰座位、连环炸弹不再惩罚已淘汰玩家、7 牌不可再与已淘汰玩家换手、剩 2 名存活玩家时转向按 2 人规则处理（等同禁止）。

## [0.13.0] - 2026-07-26

### 新增

- 音乐库新增三首原创曲目（Claude Fable 5 作曲，曲库总数达 19 首）：
  - 大厅曲 **Carousel**：G 大调旋转木马圆舞曲，曲库首支 3/4 拍——三角波主旋律配「蓬-恰-恰」华尔兹伴奏，轻鼓点，约 90 秒。
  - 游戏曲 **Showdown**：E 小调紧张对决，锯齿波主音走 Em–C–D–Bm 暗色进行，三连驱动低音，中段 breakdown 蓄力后进入高八度高潮，约 58 秒。
  - 游戏曲 **Cardsharp**：D 小调摇摆爵士，曲库首支三连音网格摇摆律动——爵士 walking bass 每拍走音、和弦只点反拍切分、ding-ding-a 摇摆镲，约 69 秒。
- 音高常量库补充 C#（Cs3/Cs4/Cs5），支持含升 C 的和声（如 A7）。

### 优化

- 结算板视觉重构：表格改为排名列表——前三名奖牌色名次徽章、冠军头像戴皇冠角标、自己带「你」标识，准备状态改为绿色对勾徽记，分数加大加粗右对齐。
- 对局玩家列表（桌面右上角面板）重构：玻璃面板外观加宽、当前回合玩家左侧主色指示条 + 头像描边高亮、手牌数改为迷你卡片徽记且剩 1 张时红色 UNO 告警、断线玩家头像角标红点。
- 个人信息弹窗视觉重构：内容按分组卡片组织（图标标题区块），昵称编辑、字号与间距全面精修。
- 玩家头像统一抽取为公共组件：真实头像 > Bot 难度配色 > 序号 emoji 三级兜底，对局各处显示一致。
- 音乐库全部曲目署名补全为完整模型名（Claude Opus 4.6）。

### 修复

- 游戏曲 Pulse 鼓轨长度为音轨两倍，导致鼓点与段落错位、后一半鼓谱永远播不到的问题；修正后鼓组编排（三段递进 → 全静音 → 三段递进 → 收尾 fill）与音轨段落逐段对齐。

## [0.12.0] - 2026-07-26

### 新增

- 卡面主题：内置复古经典（彩底白斜椭圆）、极简扁平（纯色大数字）、霓虹暗黑（本色染色深底 + 柔光）三套自绘卡面，加上默认共四套，大厅设置中一键切换，移动端同样可用；自定义资源包上传保留（支持 svg）。卡面数字使用游戏同款 Fredoka 字形（构建期转路径），全设备渲染一致。
- 终局展示窗：最后一张牌打出后保留牌桌 10 秒，横幅展示赢家与最后一张牌，期间可继续向玩家扔表情，可点「查看结算」跳过；与结算板「开始下一轮」冷却完全重叠，整体节奏不变。
- 可扔表情新增 🐷（粉色溅射 + 旋转飞行）。

### 优化

- 转向牌图标从 ⟲ 改为经典双弯箭头样式，与禁止 ⊘ 不再形近；牌面中心与四角统一更新。
- 内置卡面主题选中后刷新自动恢复（原自定义资源包为会话级，刷新即失效）。

### 修复

- Passkey 在反向代理部署下注册/登录验证必然失败的问题（rpID/origin 改为从服务端配置推导，不再依赖请求头），并补充验证失败日志。

## [0.11.0] - 2026-07-26

### 新增

- 移动端玩家罗盘：所有玩家沿弧线排布，当前回合玩家自动转到弧顶居中，直接复用桌面端玩家组件（头像/手牌数/最近出牌/+N/倒计时环/语音状态），两端同步更新。
- 移动端手牌手势：按住牌稍作停留后横向滑动选牌（经过的牌依次抬起、边缘自动滚动），上滑把牌拖出手牌区松手打出（未过线松手弹回，防误触）；双指捏合调整手牌间距。
- 移动端观战全新布局：底部控制坞集中操作，观战列表/玩家列表整合进信息面板。
- 游戏字体更换为自托管 Fredoka Variable 可变字体（此前依赖系统 Comic Sans MS，多数环境实际无该字体），跨平台显示一致。
- 个人设置弹窗改为 Tab 分类导航（通知/安全/API Keys），结构更清晰。
- 大厅主面板内直接展示「正在进行的对战」列表并可点击观战（原为仅超宽屏可见的侧边浮动面板）。

### 优化

- 前端 UI 全量重构：语义化设计 token + 统一组件（Modal/Input/Switch/Tabs/IconButton），全部弹窗统一骨架，开关、输入框、图标按钮视觉与交互一致。
- 屏幕适配改为游戏式整体等比缩放：对局牌桌（桌面 1200×720 逻辑画布）、房间准备页、大厅/登录页（上限提升至 100%）在任意分辨率下布局不变形、不裁切。
- 移动端对局布局从零重设计：罗盘 + 中央牌区 + 全新手牌区分区布局。
- 动画系统重写：投掷道具、摸牌、出牌、聊天气泡统一走视口特效层，飞牌未落地时手牌不占位、弃牌堆仍显示上一张，动画与状态不再割裂。
- 前端性能大幅优化：稳态主线程开销降低约 5 倍（金色呼吸脉冲合成器化、方向虚线/待选色脉冲 CSS 化、倒计时圆环 250ms 步进刷新），并修复全屏特效持续重栅格化导致的移动端发热（栅格化线程负载再降约 50%）。
- 回合最后 5 秒大号倒计时改为跟随「最上方玩家与牌堆中点」动态定位，不再遮挡牌桌中央。
- 桌面顶栏（HUD）在窄屏下整体等比缩小，不再拥挤换行。
- 桌面端手牌在极端牌数下不再溢出屏幕。
- 游戏信息抽屉打开时牌桌自动让位，不再遮挡玩家。
- 大厅状态栏、服务器选择弹窗、对局 HUD 三处延迟配色统一为共享实现。
- 头像裁切弹窗增加标题栏与标准关闭按钮。
- 服务端 socket 事件处理器异常兜底，客户端发送异常参数不再导致进程崩溃。

### 修复

- 一局结束开新局后移动端玩家环挤压变窄（刷新才恢复）的问题。
- 通知权限弹窗不再每次登录重复弹出，关闭后可在「设置 → 通知」中手动调整。
- Passkey 登录按钮图标和文字换行问题。
- 个人设置弹窗切换 Tab 时高度跳动。

## [0.10.0] - 2026-05-22

### 新增

- Passkey (WebAuthn) 免密码登录：已有用户可在个人设置中绑定 Passkey，之后使用指纹、面容或安全密钥一键登录。
- Passkey 管理：在个人资料弹窗中添加、查看和删除已绑定的 Passkey。
- WebAuthn 多域名支持：`WEBAUTHN_RP_ID` 和 `WEBAUTHN_ORIGIN` 支持逗号分隔配置多个域名。

### 优化

- 全局视觉重构：主题色系从蓝灰换为深暗蓝黑 + 金色体系，统一 CSS 变量。
- 新增 FitScaler 等比缩放组件，登录/注册/大厅页面自动适配屏幕尺寸。
- 大厅页面重构：品牌标题渐变金色、glass-panel 操作面板、更大的按钮和输入框。
- 注册页面重构：表单字段加左侧图标，统一输入框样式。
- 游戏房间页面优化：房间码标签化、按钮胶囊样式、设置面板毛玻璃效果。
- 响应式优化：顶栏、状态栏、用户胶囊等组件移动端适配。
- 弹窗防溢出：多个弹窗组件加窄屏内边距保护。

## [0.9.1] - 2026-05-20

### 修复

- 加入房间后座位和操作按钮不显示，需多次刷新才能恢复。
- 游戏开始后部分玩家误入观战视角，需刷新才能变为玩家视角。
- 回合结束踢掉线玩家后，观战席出现"幽灵"在线观众无法清除。
- 回合结束阶段关闭浏览器的玩家不会自动投票下一轮。
- Turnstile token 过期后未清空，导致提交失效 token。

## [0.9.0] - 2026-05-19

### 新增

- 客户端更新检测：Caddy 容器重启后自动提示刷新，不再仅限服务端更新。
- 头像裁切支持旋转：自由拖动滑块设置任意角度，90° 快捷按钮保留。
- 注册/登录安全增强。

### 优化

- 管理后台移除 shadcn/ui 依赖，精简打包体积。
- 提取菜单样式常量、移除冗余处理器、优化 fetchSockets 调用。
- Bot 名字库从每字母 1 个扩充到 6 个（共 156 个）。
- 移除 Docker 镜像中多余的 sharp musl 二进制文件。
- 同步 protocol.md 至当前代码实现。

### 修复

- 头像裁切框显示空白（blob URL 被提前释放）。
- Bot 难度标签换行问题。
- 积分板中 AI 机器人显示为已准备状态。
- 统一浮动面板样式、修复拖拽约束与全局交互限制。

## [0.8.0] - 2026-05-19

### 新增

- 游戏计时器：回合计时与玩家延迟实时显示。
- 移动端游戏布局适配与托管遮罩。
- 可拖拽悬浮面板、玩家列表折叠及观战流程优化。
- 观战者 KV 生命周期统一与断线处理。

### 优化

- 移除"死亡抽牌"村规，功能与"摸到能出为止"重复，统一使用后者。
- 观战房主回合结束时自动排队入座，加入按钮改为三态。
- 房主迁移完善：主动离开即时迁移、移交不限阶段、观战者房主提示。
- 房间重连稳定性修复与连接速度优化。
- 返回房间流程修复、踢人简化、房主迁移统一及超时优化。
- 通知权限和更新日志弹窗仅登录后显示。

### 修复

- 修复末牌限制/空手赢不算触发时不补摸导致对局卡死的问题。
- 修复游戏规则、语音和登录稳定性相关问题。

## [0.7.0] - 2026-05-16

### 新增

- 房间座位系统：10 个固定座位围绕圆桌排列，座位顺序决定游戏上下家。
- 选座入座：新玩家加入默认进入观战席，点击空座位入座。
- 换座功能：玩家间请求交换座位（需对方确认，15 秒超时）；与 Bot 直接交换。
- 换座冷却：发起方 5 秒冷却，防止滥用。
- 座位右键菜单：空位可入座或添加指定难度 Bot；Bot 位可换座、调难度、移除。
- 断线标记：已入座玩家断线显示断线图标，自动取消准备，60 秒内可重连恢复座位。
- 设置抽屉：观战设置和村规设置移至右侧滑出面板，释放主视觉空间。
- MCP 新增 `take_seat` / `leave_seat` 工具。

### 优化

- 房间页面从列表布局改为圆桌布局，移动端自适应缩放。
- 观战席改为圆桌下方横排显示。
- Bot 头像统一使用 lucide 图标，与游戏内一致。
- 添加 Bot 支持指定座位，不再仅追加到第一个空位。
- 村规 `shuffleSeats` 适配：返回房间时随机打乱已占座位。
- Room key 扫描逻辑改为按 `:` 提取房间 code，对新增子 key 更鲁棒。

### 修复

- 修复调整 Bot 难度后 UI 不刷新的问题。

## [0.6.0] - 2026-05-16

### 新增

- Bot AI (人机) 功能：支持添加 Bot 填充空位，Bot 具备完整 AI 决策逻辑。
- Bot UI/UX：单步托管动画、Bot 标识、Bot 管理面板。
- 大厅房间列表改为 Socket.IO 实时推送，移除 HTTP 轮询。
- 服务器延迟测量改用 Socket.IO ping/ack 往返，精度更高。

### 优化

- Bot 摸牌改为快速模式，减少等待时间。
- 房主迁移逻辑优化：Bot 不会成为房主。
- 清理 Bot UI 重复代码及冗余调用。
- 延迟测量间隔调整为 30 秒，减少网络开销。
- 移除 `/api/rooms/active` HTTP 接口（已被 Socket.IO 实时推送替代）。

### 修复

- 观战房主离开后仅剩 Bot 时正确解散房间。
- Bot 回合结束阶段自动投票并支持直接移除。
- 未登录时延迟测量回退到 HTTP 方式。

## [0.5.1] - 2026-05-15

### 优化

- 服务端：Autopilot/Bot 每次操作从 2-6 次全量广播降至 1 次，大幅减少网络开销。
- 服务端：弃牌堆传输截断为最近 10 张，避免随游戏进程包体线性增长。
- 客户端：合并 socket 事件中两次独立的 store 更新为一次，减少 re-render。
- 客户端：players/discardPile 添加浅比较，内容未变时复用旧引用避免无意义 re-render。
- 客户端：方向指示器从 Framer Motion JS 动画改为 CSS GPU 合成动画，释放主线程。

### 新增

- 更新日志弹窗默认只显示最近 3 个版本，可展开查看更早版本。

## [0.5.0] - 2026-05-15

### 优化

- 全量依赖升级：React 18→19、Vite 6→8、TypeScript 5→6、Vitest 3→4、Tailwind CSS 4.2→4.3、Zod 3→4、Kysely 0.28→0.29、@vitejs/plugin-react 4→5、@modelcontextprotocol/sdk ^1.12→^1.29。
- 移除未使用的 `@octokit/oauth-app` 依赖。
- 移除已废弃的 `@types/bcryptjs`（bcryptjs 3.x 自带类型）。
- MCP 包发布到 npm（`@uno-online/mcp@0.4.0`），MCP 发包流程写入 CLAUDE.md。
- 客户端更新日志补全、GitHub 链接、头像替换字母、图标替换 emoji。

### 修复

- React 19 类型适配：`useRef` 调用补充初始值参数。
- TypeScript 6 适配：移除 tsconfig 中已废弃的 `baseUrl` 选项。
- 服务端测试从直连 Redis 改为 `MemoryKvStore`，不再依赖外部 Redis 实例。
- 观战测试断言同步适配 `getSpectatorNames()` 返回 `SpectatorInfo[]` 对象格式。
- 文档版本号同步更新（README、前后端开发规范、CLAUDE.md）。

## [0.4.0] - 2026-05-14

### 新增

- MCP Server（`packages/mcp`）：AI 客户端可通过 MCP 工具加入游戏，支持房间管理、游戏操作、状态查询。
- 8-bit 程序化 BGM 系统：大厅音乐、房间 BGM、音乐厅面板。
- 观众席全面改版：可视化观众列表、观众丢表情与抓 UNO、积分板加入/退出按钮、多 tab 支持。
- 双牌堆 + 弃牌堆散开布局，支持横竖屏自适应。
- 抢出村规：允许在他人回合打出完全相同的牌。
- 随机座位村规。
- 牌堆耗尽自动洗牌回收弃牌堆。
- 连续超时自动开启托管模式。
- 回合结束踢出未准备玩家，踢出改为移至观战席。
- 反作弊系统：服务端作弊检测 + 客户端全屏警告 UI。
- 玩家操作菜单：移交房主、踢人、强制静音、音量调节。
- 浏览器通知：游戏开始、轮到你、游戏结束、被踢出。
- 服务器版本更新提示。
- AI 玩家标识徽章。
- 选色光波全屏效果、+4 接受时广播全屏效果。
- 新手教程引导。
- Mumble ICE 频道管理：房间自动创建/销毁语音频道。
- 语音频道加入/离开提示音。
- game over 后可返回房间继续、房间内观战席。
- 房主可进入观战席、退出房间改为转让房主。
- 页面内 modal 替代浏览器原生 confirm/alert。
- 开始界面 overlay + 登录注册页面重设计。
- Enter 键快捷打开聊天框。

### 优化

- Docker 镜像体积 730MB → 252MB（迁移 NodeNext 模块解析）。
- 游戏信息展示改善：TopBar 牌面状态、日志补全、托管节奏优化。
- 回合冷却倒计时改为独立进度条，仅限制房主开始游戏，不影响玩家投票。
- 客户端时钟校准：连接时同步服务器时间偏移量，修复客户端时钟偏差导致倒计时异常。
- 观众席弹幕、聊天布局优化。
- 玩家与观战席分列显示，MAX_PLAYERS 不再计入观战席。
- 房间页按钮文字大小统一 + 移动端布局适配。
- 房间加入/离开状态原子化 + 客户端 teardown 统一。
- Caddy 配置抽取 SPA snippet + 默认安全头与健康检查。
- 移除游戏记录/回放系统、字体选择与圆角切换等低使用率功能。

### 修复

- 修复快速模式在质疑、选色、交换等阶段未生效的问题。
- 修复选颜色阶段被抓 UNO 后无法继续选色。
- 修复变色 +4 颜色状态异常。
- 修复罚摸期间可提前跳过的问题。
- 修复托管模式下抢出、万能牌选色、+4 卡死等多个 bug。
- 修复最后一张功能牌不触发结算胜利的问题。
- 修复质疑阶段叠加 +4 时不弹选色器导致游戏卡死。
- 修复对局记录不保存、rematch 无权限检查及断线处理逻辑。
- 修复观众离开时 spectators 列表崩溃。
- 修复自己的语音说话指示器不亮。
- 修复 GitHub OAuth 代理不生效（User-Agent 头、undici request API）。
- 修复观战席同步、表情连发节流、摸牌音效、抓 UNO 日志。
- 修复 modal 关闭缺少淡出动画、音乐厅与 BGM 状态不一致。
- 修复游戏结束后房主无法踢人 + 观战者被强制拉到玩家席。
- 修复观众主动离开未同步清理列表 + 重连解散时的注册表泄漏。

## [0.3.2] - 2026-05-10

### 新增

- 房主之外的玩家在回合结算后点击“下一局”改为投票，票数过半才开始下一局；房主仍可直接开始下一局。
- 弃牌堆改为完整状态同步，并在桌面中央展示最近 8 张的堆叠效果和 `弃牌堆 (N)` 数量。
- 所有罚摸和给牌改为逐张摸牌，并增加从牌堆飞向玩家手牌区/头像小手牌区域的动画。
- 头像旁新增本局获胜回合数和积分展示；无头像时使用 emoji 作为兜底。
- 手牌增长时在头像小手牌区域右侧显示 `+X` 滚动提示。
- UNO 和抓 UNO 增加全屏特效；抓 UNO 特效会显示谁抓了谁，并同步写入游戏日志。
- 游戏内补充音效反馈：普通出牌、罚牌、被扔道具命中都会播放音效；被扔道具会按道具类型使用不同卡通命中音。
- 房间增加闲置自动解散机制，`ROOM_IDLE_TIMEOUT_MS` 可通过 `.env` 或 `docker-compose.yml` 配置。
- 房间聊天记录会随游戏事件保存，重连、观战和回放时可以恢复显示。
- 复制房间链接、粘贴房间 URL 加入、GitHub OAuth 代理配置。
- 管理后台新增游戏记录管理。

### 优化

- “摸到能出为止”村规改为逐张摸，并在牌堆下方显示已摸张数。
- 被加牌时提示文案从“罚摸 X 张”调整为“还要摸 X 张”。
- 游戏日志合并连续摸牌记录，例如 `玩家4 摸牌 x6`，并修复 +4 重复记录问题。
- 方向圆环、虚线高亮和椭圆箭头按当前出牌方向运动，并加宽箭头。
- +2/+4 触发后增加半秒停顿，让节奏更清晰。
- 宽屏布局下右侧入口面板和游戏内容宽度适配更稳定。
- GitHub 登录按钮亮度提高，操作按钮点击区域修正。
- 语音频道成员显示玩家昵称，不再显示 `player_id`。
- 语音解码器关闭后的错误降噪，减少部分玩家听不到声音的问题。
- 文本选择在游戏页被屏蔽，避免拖拽/双击误选 UI。
- 村规可出牌判断提取为共用逻辑，并改为早停判断，减少重复遍历。
- 游戏全屏特效创建和自动移除逻辑去重，后续新增特效更容易维护。
- 项目文档和仓库链接迁移到 `letsuno` 组织，并清理过期计划/规格文档。

### 修复

- 修复房间失效后仍留在大厅列表的问题；列表会清理 `playing` 但游戏状态丢失的死房间。
- 修复结算页点击“返回大厅”只前端跳转、不离开房间导致死房间的问题。
- 修复房主返回大厅/解散房间后，其它玩家没有被自动退回大厅而卡空白页的问题。
- 修复被跳过回合后仍显示上一张出牌标记的问题。
- 修复叠加罚摸后无法继续摸牌的问题。
- 修复变色 +4 不能选颜色的问题。
- 修复打别人未喊 UNO 的罚牌可以重复触发的问题。
- 修复功能牌效果展示显示到下一家而不是被触发玩家的问题。
- 修复上传头像被 emoji 覆盖的问题。
- 修复“手牌透明”村规不生效的问题，低于阈值时头像下小手牌会展示真实牌；观战 hidden 模式也尊重该规则。
- 修复观战会话失效或恢复时可能无法正确进入观战状态的问题。
- 修复观战会话恢复构建失败的问题。
- 修复叠加罚牌规则下部分剩余手牌类村规的摸牌校验问题，并补充对应测试。
- 修复语音面板部分控制台 `AudioDecoder` closed codec 报错。
- 修复管理员面板 DELETE 请求、`/admin` 重定向、主站登录态复用、用户不存在时接口返回码等问题。
- 统一前端使用后端 API 路径，不再保留旧路径兼容。

## [0.3.1] - 2026-05-07

### 新增

- 手牌少于 8 张时，在头像下方展示小卡牌背面图示代替数字显示
- 游戏日志改为最新记录在上方，新回合之间显示分割线标记
- 替换 mediasoup 语音为 mumble-web gateway 架构（WebSocket 网关 + Mumble 服务器）
- 语音说话人头像绿色发光高亮、分人音量调节
- 服务器状态按钮自动每 30 秒刷新
- 扔道具和聊天速率限制大幅放宽

### 优化

- Dockerfile 使用大陆镜像源（apt 阿里云、pnpm npmmirror）加速构建

### 修复

- 修复出牌前喊 UNO 被出牌动作覆盖的 bug：出牌后剩 1 张牌时保留 `calledUno` 状态
- 修复跳过覆盖层仅检测 `skip` 卡的问题：现在 `draw_two` 和 2 人局 `reverse` 也会显示跳过效果
- 修复跳过覆盖层不会自动刷新的问题：下一个动作到来时自动清除，不再仅依赖固定超时
- 修复房间玩家重复显示的 bug：三层防御（store 层去重、事件层去重、客户端去重）
- 修复叠加村规下可用普通万能牌逃脱 +2/+4 惩罚的 bug：drawStack > 0 时只允许叠加牌打出

## [0.3.0] - 2026-05-07

### 新增

#### 管理后台

- 独立管理后台应用（`packages/admin`），支持用户管理、房间管理、数据统计
- 管理端 API 插件（`/admin/*`），包含 `adminOnly` 权限中间件
- 首个注册用户自动升级为管理员
- Dashboard 统计面板：总用户数、总游戏数、活跃房间数
- 用户管理：搜索、分页、角色修改
- 房间管理：查看活跃房间、强制解散

#### 用户角色系统

- 四级用户组：普通（白色/5s冷却）、会员（绿色/2s）、VIP（金色/1s）、管理员（红色/无限制）
- 角色颜色贯穿全局：游戏内昵称、聊天、房间列表、个人资料
- 基于角色的扔道具冷却时间差异化

#### 游戏体验

- 椭圆轨道高亮弧段 + 箭头指示出牌方向
- 牌堆下方回合指示器：显示当前操作玩家头像、昵称、倒计时
- 阶段感知提示文案：质疑+4、选色、交换等状态显示对应操作文字
- 摸牌动画方向修正：卡牌飞向实际摸牌玩家位置，而非固定向下
- PC 端右上角玩家列表面板：显示所有玩家状态、手牌数、在线状态
- 游戏日志可折叠/展开
- Google 风格四色头像边框

#### 托管模式增强

- 自动质疑 +4（`challenging` 阶段）
- 自动选色（选手牌最多的颜色）
- 自动选择交换对象（选手牌最多的玩家）

#### 主题系统

- 圆角/科技双风格主题，支持一键切换
- 主题感知的 CSS 变量（`--radius-btn`, `--radius-input`, `--radius-card-ui`, `--radius-panel-ui`）
- 科技风格额外边框发光效果
- 统一 Input 组件（cva 变体：default/ghost，尺寸：default/lg/sm）
- Button 组件新增 `ghost` 和 `outline` 变体

#### 构建信息

- 首页左下角显示版本号和构建日期
- 游戏 TopBar 显示版本号

#### 文档

- CLAUDE.md 项目总览
- 前端开发规范（目录结构、命名、组件、样式、状态管理）
- 后端开发规范（插件架构、路由、数据库、安全规范）
- 插件扩展规范（服务端/客户端模板、开发流程）
- 村规扩展指南（Rule 注册表模式、RuleDefinition 接口）

### 修复

- 修复 `/profile` 接口泄露 `passwordHash` 到客户端的安全漏洞
- 修复服务端测试文件 import 路径（适配插件目录结构）
- 修复 `chat:message` 字段命名：`username` 改为 `nickname`（语义一致）
- 修复质疑 +4 阶段回合指示器显示错误玩家（应显示 `pendingDrawPlayerId`）
- 修复 `choose_color` 进入 `challenging` 后计时器被停止的 bug
- 服务端超时兜底：质疑/选色/交换阶段超时自动执行默认操作，防止玩家离线卡死

### 重构

- **服务端插件架构**：Auth、Profile 迁移为 Fastify 插件；Room、Game、Voice、Interaction 模块移入 `plugins/core/` 目录
- **客户端 Feature 模块**：所有页面/组件/store 按功能域重组为 `features/`（auth、game、lobby、profile）+ `shared/`
- 创建 `PluginContext` 和 `plugin-loader` 基础设施
- 统一 `SocketData` 接口到 `ws/types.ts`
- 创建 `useEffectiveUserId` hook 消除 7 处重复的 `viewerId ?? authUserId` 模式
- 清理死代码：`shared-state.ts`、`createAuthHook`、`refreshGameStateTTL`、`cleanupRoomVoice`
- 移除不必要的 export、精简 `kv/index.ts` 重导出
- 删除废弃组件 `DirectionIndicator`、`OpponentRow`
- 删除死代码 `redis/client.ts`

## [0.2.0] - 2026-05-06

### 新增

- 用户名密码注册登录、昵称系统、头像上传
- 椭圆圆桌布局：玩家节点环绕排列，倒计时圆环
- 聊天气泡、快捷反应、快捷短语、扔道具互动
- 村规信息卡片、游戏日志面板
- 扑克风格卡角、扇形手牌布局、自动排序、发光条
- 扔道具事件和聊天频率限制
- 字体选择、卡面资源包加载
- 用户头像系统
- 房间重连和资源包支持
- 抽牌动画
- 自动托管（Bot）
- 头像缓存优化（ETag/304）
- 运行时配置（DEV_MODE、GitHub Client ID）

### 变更

- 迁移全部样式到 Tailwind CSS v4 统一暗色主题
- 移除 TypeScript import 中不必要的 `.js` 扩展名
- Redis 改为可选依赖
- 移除 Prisma，改用 Kysely + SQLite

### 修复

- 修复 UNO 游戏逻辑、状态同步与手牌显示问题
- 修复方向弧线 SVG 绘制重叠
- 修复卡角显示、手牌悬停动画
- 修复扔道具坐标计算（容器相对 → 视口坐标）
- 修复菜单被 overflow-hidden 裁剪（Portal 渲染）

## [0.1.0] - 2026-05-06

### 新增

- UNO Online 多人在线卡牌游戏初始版本
- React 客户端 + Node.js 服务端 + 共享游戏逻辑
- 开发模式：跳过 GitHub OAuth，仅用户名登录
- 服务端启动自动同步数据库表结构
- 登录重定向保持房间 URL
- 游戏交互打磨和 UX 反馈

### 修复

- 修复用户刷新页面丢失状态
- 修复游戏重连和出牌守卫
- 修复房间状态 bug：房主村规禁用、刷新丢失玩家
- 修复 Caddy 路由：区分 Auth API 和 SPA 回调页面

### 变更

- 默认全部部署使用 HTTPS
- 从 DOMAIN 推导 CLIENT_URL

### 基础设施

- 使用 Caddy 替换 nginx，支持自动 SSL
